{
  "name": "milksales-smart-claims",
  "version": "1.0.0",
  "description": "A simple CAP project.",
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "@sap/cds": "^9",
    "axios": "^1.14.0",
    "dotenv": "^17.3.1",
    "express": "^4"
  },
  "devDependencies": {
    "@cap-js/sqlite": "^2"
  },
  "scripts": {
    "start": "cds-serve"
  },
  "cds": {
    "requires": {
      "db": {
        "kind": "sqlite",
        "credentials": {
          "url": "db.sqlite"
        }
      }
    }
  },
  "private": true
}
